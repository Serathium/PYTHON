# Creating Virtual Environment  
Inside the project's directory, execute:  
`python3 -m venv .venv`  
  
# Activating Environment  
Everytime the terminal is used to work on the project, execute:  
`source .venv/bin/activate` 
